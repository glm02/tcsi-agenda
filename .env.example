# Environment Variables for GEII-OS Node.js Server & Telegram Bot

# Telegram Bot Token (Get it from @BotFather on Telegram)
TELEGRAM_BOT_TOKEN="8246101011:AAEPf5waVaVQN5iBvrDattPVHaEegAnUp2Q"

# Supabase URL and Key (If not already configured via Vite and visible in .env)
# The React side usually uses VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY
# If your backend needs it, declare them here as well.

# Server Port (Railway will automatically inject the PORT, but you can set it here for local testing)
PORT=3000
